# Forecast vNext — 適応学習（Adaptive Learning）設計方針

最終更新: 2026-08-20  
対象: 管理ハブ／クライアント年度ブック／Engine／横断プール  
本書は「数字当てゲーム」ではなく、**適応・学習・進捗のプロセス**を本丸とする。

### UI 制約（Web入口）

- **大きく変えない:** 01〜03 の Bot／キャラ付き吹き出し導線、「キャラと対話している」画面構成。
- **変えてよい:** 配色、ボタン名、説明文、ボタン追加、機能の増減・変更。
- 理由: 見た目の刷新より、気に入っている対話感の踏襲が優先。学習機能の画面も、可能なら同系統の案内トーンを流用する。

---

## 0. 目的・非目的・代理目的

### 0.1 本丸（直接は最適化しない）

- 組織がクライアント×FYの不確実性を、**観測・仮説・検証・次の予測への反映**として回し続けられること。
- 外れた／当たった要因が**蓄積**され、次の予測 run の数値・振れ幅・注意が**実際に変わる**こと。
- 人が理解・監査・引き継ぎできること（完全自動のブラックボックス最適ではない）。

### 0.2 非目的（やらない／混ぜない）

- 正式予算・営業上積み・採用判断差分を学習に戻すこと（現状契約どおり）。
- 過去の公式 vintage を後から書き換えて「当たったように見せる」こと。
- 年度途中の**会社予算の補正**（運用上ほぼ行わない）。途中は観測・仮説・検証・情報用予測のみ。
- 人が理解できない構造探索の自動適用。
- Goodhart 化（単一の点予測誤差だけを直接最大化）。

### 0.3 代理目的（統計的優先順位・推奨）

未来予測の真の目的は直接最適化できない。代わりに次の代理指標をこの順で追う。

| 順位 | 代理目的 | なぜ先か | 主な監視 |
|---|---|---|---|
| **1** | **区間校正（calibration）** | 本システムは一点ではなく分布を出す。P10–P90 の的中率が「想定内／想定外」の基盤になる。点誤差だけ追うと振れ幅を潰して見かけ精度を稼ぎやすい（Goodhart）。 | FY／四半期の実績が予測区間に入ったか。系統的に狭すぎ／広すぎないか |
| **2** | **層別バイアス（attribution）** | 「重みの強い変数が間違っていた」を測る。継続性／案件／現場／AI／SPOT 等の残差寄与。学習の対象を特定できる。 | 層別 mean error、reliability の hit/n、高寄与ソースの系統誤差 |
| **3** | **点予測誤差（制約付き）** | 経営が感覚的に見る指標。ただし **1・2 を悪化させない制約下**でのみ改善を認める。 | FY合計の相対誤差／絶対誤差。月次は診断用 |
| **4** | **情報ギャップ縮小** | 「わからないまま外した」割合を減らすプロセス指標。精度そのものではないが適応の進捗。 | UNKNOWN／未検証仮説の残存、次情報リストの消化 |

**ダッシュボードに出す主指標:** 1 → 2 →（要約としての）3。  
**学習アルゴリズムが直接更新してよい内部状態:** 主に 1・2 に効く量（振れ幅校正、reliability、案件確率などの状態）。  
**構造・係数の Model Release:** 人ゲート（閾値超過・重要変更のみ）。

---

## 1. 評価の二系統（両方見る）

| 系統 | 比較対象 | 用途 |
|---|---|---|
| **System track** | システム推奨予測 vs 確定実績 | **学習に使う**。状態更新・Model candidate の根拠 |
| **Budget track** | 正式予算 vs 確定実績 | **経営・監査用**。学習に戻さない。途中補正しない前提のギャップ可視化 |

年度途中: 正式予算は不変。月次で実績・案件を観測し、仮説／検証イベントを蓄積する。必要なら **情報用の新 system run**（immutable）を残す。

**想定外アラート（推奨）:** 確定実績（または強い途中実績）が現行公式 vintage の P10–P90 を大きく外れた、または層別残差が閾値超え → Hub／担当へ現場確認アラート。予算は書き換えない。

---

## 2. 時間軸

| 周期 | 内容 |
|---|---|
| **月次** | 実績取込（翌月10日前後）・外因仮説の記録・区間外れアラート・情報用再予測（任意） |
| **四半期** | reliability／校正の候補生成、閾値超えだけ人承認、横断プールへの生証拠集約 |
| **年次** | 本格振り返り（原因カテゴリ・次情報・公式 vintage 評価）、次FY Template/Model への反映 |

---

## 3. 人の介入ゲート（推奨確定）

- **自動:** 測定、生証拠の永続化、候補生成、閾値未満の状態更新、情報用 run。
- **人承認が必要:** 閾値超過の状態更新、Model Release（Engine／係数／ルール）、重要ポリシー変更。
- **常に分離記録:** システム由来データと人間補正・現場情報は別レコード。混ぜて学習しない。

---

## 4. 蓄積するもの

1. **客観数値（最優先）** — 誤差、区間的中、層別寄与、reliability の hit/n  
2. **仮説・検証ログ** — 月次／四半期の「なぜ外れたか」仮説と検証結果  
3. **原因ラベル＋次情報** — 年次振り返り  
4. **領域タグ（設計のみ先行）** — オンコ／自己免疫など。ZAC 製品情報からの AI 仮分類（精度は中程度で可、再分類容易）。予測コアへの直結は後回し。ポートフォリオ切り口として拡張余地をスキーマに確保

単位: **クライアント別** ＋ **横断プール** ＋（将来）**領域**。

---

## 5. 「フィードバックが動いている」の判定

最低限の成功条件（理想）:

1. 次の予測 run の **点または振れ幅が、学習前後で実際に変わる**（同一入力でも校正後は変わりうる／または明示イベントが入力に入る）  
2. Hub に **何が変わったか**（どの状態・どのソース・どの閾値）の要約が出る  
3. 感覚論で終わらせず、代理指標 1→2→3 が追跡できる

入力画面に残るだけでは不十分。

---

## 6. ベイジアンへの段階計画

| Phase | 内容 | ベイジアン対応 |
|---|---|---|
| **A — 配線と証拠** | 振り返り／評価／月次仮説が Hub に残り、**次 run に効く**ことを検証。hit/n・層別残差の永続化。Budget track と System track の分離ダッシュボード | 尤度に相当する生証拠を捨てない |
| **B — 状態の事後更新** | 振れ幅・reliability・（既存の）案件関連状態を、閾値ゲート付きで自動更新。四半期候補＋年次本格。横断 POOL prior | 構造固定の **状態事後更新**（実用ベイズ） |
| **C — 候補モデル重み** | 複数 Model candidate の重み／選択を実績で更新し、**有効化は人**。構造探索の自動適用は禁止 | モデル平均／選択のベイズ的更新 |
| **D — 領域・ポートフォリオ** | AI 仮分類＋再分類 UI。領域別の診断ビュー（予測コア直結は任意） | 階層 prior の切り口追加 |

重い専用学習基盤は必須としない。Sheets＋Hub 集約＋既存 Engine 状態更新を優先。コスト急増する基盤は Phase C 以降で要否判断。

---

## 7. 記録・ダッシュボード・エクスポート

| 層 | 置くもの |
|---|---|
| **リポジトリ** | 本設計・契約・スキーマ |
| **Obsidian** | 判断経緯・ADR |
| **Hub 非表示シート／設定** | 前提・代理目的定義・重要統計前提値・仮説・計算過程の正本（普段の現場画面には出さない） |
| **簡易ダッシュボード** | 概念図（未来予測→代理目的→モデル化）、重要統計前提の要約、代理指標 1–3、直近の「何が変わったか」 |
| **詳細エクスポート** | 監査・引き継ぎ・他システム移管用の JSON／シート一式 |

現場メンバー向け UI に細かい前提を並べない。必要なときに Hub 管理者／監査ビューから辿れること。

---

## 8. 現状ギャップ（洗い出し・要約）

実装・設計の既存資産:

- 公式 vintage の凍結、評価・振り返り UI、学習ペイロードから採用／上積みを除外する契約
- Engine 側の区間校正（regime／MAD shrinkage 等）
- legacy 系の reliability／POOL_PRIOR 構想（vNext Hub との統合は未完了の可能性大）
- Model Release の人 attestation（自動評価への置換は未）

### Phase A 実装状況（2026-08-20）

コード配線は完了。Hub 非表示シート `LEARNING_OBSERVATION` / `LEARNING_EVIDENCE`、Settings の `LEARNING_POLICY_JSON`、サイドバー「適応学習」カード、振り返り→証拠→次 run の `logSigma` 拡大（区間ミス時）まで接続済み。自動テスト: `tests/vnext-learning-phase-a.test.mjs`。

| 項目 | 状態 |
|---|---|
| LEARNING_POLICY 正本＋簡易ダッシュボード | **実装済**（Hub SETTINGS + Admin Sidebar） |
| System / Budget 二系統ビュー | **実装済**（ダッシュボード dualTracks。Budget は学習除外） |
| 月次観測＋区間外れアラート | **実装済**（LEARNING_OBSERVATION + EXCEPTION） |
| 層別残差の永続化→次 run | **Phase A 最小**（証拠 JSON 永続化＋区間ミス時 widen。層別自動更新は Phase B） |

主なギャップ（残り）:

1. ~~月次の仮説・検証イベント~~ → Phase A で第一級化済み（検証ステータス運用は現場定着が次）  
2. **層別バイアス／reliability の自動状態更新**（証拠はあるが Engine への層別反映は Phase B）  
3. ~~Budget vs System ダッシュボード~~ → Phase A で要約表示済み  
4. ~~区間外れアラート配線~~ → Phase A で EXCEPTION 連携済み（運用フロー定着は別）  
5. **閾値付き自動状態更新**のゲートが未統一  
6. **横断プール**が vNext Hub 正本として未完了  
7. ~~代理目的の正本~~ → Hub SETTINGS に書き込み済み（リポジトリ本書と同期）  
8. **領域タグ**はスキーマ予約のみで実装後回し（意図どおり）

---

## 9. 短期／長期の同時推進

- **短期:** Phase B — 振れ幅・reliability・層別状態の閾値ゲート付き事後更新、横断プール入口  
- **長期:** Phase C — ベイジアン的な候補モデル更新（有効化は人）、Phase D — 領域ポートフォリオ  

ルールベース（状態・閾値・契約）と AI（調査・領域仮分類・要約）のハイブリッド。人力入力は最小（振り返りラベル、閾値承認、重要例外）。

---

## 10. 次の実装着手順（提案）

1. ~~Hub に LEARNING_POLICY／代理目的正本・簡易ダッシュボード~~（Phase A 済）  
2. ~~System/Budget 二系統の評価ビュー~~（Phase A 済）  
3. ~~月次観測イベント（仮説・検証・区間外れアラート）~~（Phase A 済）  
4. ~~証拠永続化＋次予測への最小反映（区間 widen）と自動テスト~~（Phase A 済）  
5. **閾値付き状態更新ゲート（Phase B）** — reliability／層別バイアスの自動更新  
6. 横断プール（既存構想の vNext 統合）  
7. Model candidate の実績バインド（Phase C）  
8. 領域仮分類（Phase D）

時期の固定より、**A→B→C の計画全体が常に見えること**をゴール管理の単位とする。
