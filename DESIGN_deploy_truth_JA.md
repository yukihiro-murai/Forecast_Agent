# Cursor反映の契約（これだけ）

最終更新: 2026-08-24

## 役割分担

| 誰 | やること |
|---|---|
| **エージェント** | 編集 → test → `npm run reflect` |
| **あなた** | 下の「手順A」（画面に必ずある方） |

## 手順A（いま止まっているとき）

表示が **「2/4 Client release…」** のままなら、古い案内パネルの処理です。待たない。

1. 右パネルを **× で閉じる**（またはブラウザでシートを再読み込み）
2. 上部メニュー **「年度予算策定」→「案内を開く」**
3. 緑の大きいボタンを **もう一度**
4. Web入口を **Cmd+Shift+R**

Hub 同期のあと案内を開き直さないと、古い「2/4 Client」が続いて止まります。

## 手順A（通常）

管理ハブ Spreadsheet で:

1. 上部メニュー **「年度予算策定」→「案内を開く」**
2. **右に出るパネル**の上のほうの **緑の大きいボタン**
   - 名前は環境で違う: **「反映する」** または **「開発反映を実行」**
3. もし「案内を閉じて開き直して」と出たら、その通りにして緑ボタンをもう一度
4. Web入口を **Cmd+Shift+R**

## 手順B（メニューに項目があるときだけ）

上部メニュー **「年度予算策定」→「Web入口を最新版にする」**

この項目は Hub が最新コードに同期され、Spreadsheet を開き直したあとに出ます。  
**見えないときは手順Aを使ってください。**

## エージェントへの注意

画面に無いメニュー名だけを案内しない。手順Aを正本にする。
