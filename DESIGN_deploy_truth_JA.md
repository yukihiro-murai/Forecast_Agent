# Cursor反映の契約（これだけ）

最終更新: 2026-08-20

## あなたがやること

1. Cursor 側で編集が終わったら（エージェントが `clasp push` まで済ませる）
2. 管理ハブで次の **どれか1つ**
   - サイドバー「Cursor反映」→ **反映する**
   - またはメニュー **年度計画 → Web入口を最新版にする**
3. Web入口を **Cmd+Shift+R**

確認ボタンや、システムの手入れの奥のボタンは使わなくてよい。

## システムがやること（自動）

1. 管理ハブを中央 clasp に同期  
2. **先に Web入口（Portal）を更新**（ここがユーザーに見える変更）  
3. Client が古いときだけ Template/Model を更新  
4. 中央・Hub・Portal の版が一致するか確認  

Client 再作成を Portal より先にやると、そこで止まって Web入口が永遠に古いままになる。順序を Portal 優先に固定した。

## ✓ の意味

✓ = 中央 clasp・Hub・live Portal が同じ版。  
Hub 内だけの一致では ✓ にしない。
